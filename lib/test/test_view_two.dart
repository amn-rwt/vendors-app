import 'package:flutter/material.dart';

class TestViewTwo extends StatelessWidget {
  const TestViewTwo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('2')),
    );
  }
}
